# AI Productivity Hub - 产品需求文档 (PRD) v2.0

## 1. 文档概览

- **目标**：建立完整的用户体系，实现多设备配置同步、数据隔离及高效检索。

---

## 2. 核心功能描述

### 2.1 用户认证系统

- **注册/登录**：支持“手机号/邮箱 + 密码”多渠道认证。
- **安全性**：采用 `bcrypt` 强哈希加密。

### 2.2 用户配置云同步

- **自动保存**：AI 供应商设置及 API Key 自动同步。
- **跨端加载**：登录后自动恢复最近使用的模型配置。

### 2.3 现代化日志搜索

- **全文检索**：Dashboard 支持对内容及标签的实时模糊匹配。
- **性能支撑**：基于 `pg_trgm` 的三元组索引优化。

### 2.4 无压快记与 AI 自动聚合

- **功能点**：提供极简记录入口（Inbox），内容自动打标并分类为“笔记”或“任务”。
- **聚合机制**：系统每日 18:00 自动将 Inbox 中的零散内容通过 AI 聚类，生成结构化日报。
- **溯源性**：聚合结果必须可追溯原始记录，支持状态追踪 (`is_processed`)。

---

## 3. 技术架构

- **前端**：React + TypeScript + Tailwind。
- **后端**：FastAPI + Pydantic。
- **数据库**：Supabase (PostgreSQL) + pg_trgm。
